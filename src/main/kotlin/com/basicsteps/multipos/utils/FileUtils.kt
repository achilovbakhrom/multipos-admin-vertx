package com.basicsteps.multipos.utils


import io.reactivex.Observable

fun move(source: String, destination: String) : Observable<Boolean> {
    return Observable.create({ event ->

    })
}

fun copy(source: String, destination: String) : Observable<Boolean> {
    return Observable.create({ event ->

    })
}

fun delete(source: String) : Observable<Boolean> {
    return Observable.create({ event ->

    })
}